[pytest]
addopts = -p no:cacheprovider
markers =
    e2e: real end-to-end tests that call external APIs and require credentials
    eval_metrics: dataset-driven evaluation that calls the real pipeline and writes an HTML metrics report
    eval_deepeval: DeepEval-based dataset evaluation that calls the real pipeline and writes an HTML metrics report
